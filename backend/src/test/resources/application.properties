# Test configuration
spring.data.mongodb.uri=mongodb://localhost:27017/quizmaster-test
spring.mongodb.embedded.version=4.0.2

# JWT configuration for tests
jwt.secret=test-secret-key-for-jwt-token-generation-in-testing-environment
jwt.expiration=3600000
jwt.refresh-expiration=86400000

# Disable email sending in tests
spring.mail.host=localhost
spring.mail.port=3025
spring.mail.username=test
spring.mail.password=test

# Mock Openrouter.ai Configuration
openrouter.api.key=test-api-key
openrouter.api.url=http://localhost:8089/api/v1/chat/completions
openrouter.default.model=test-model
